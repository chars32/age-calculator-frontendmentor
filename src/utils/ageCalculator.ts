interface Age {
  years: number;
  months: number;
  days: number;
}

export const calculateAge = (birthDate: Date): Age => {
  // La lógica real del cálculo se implementará en el Paso 5.
  // Esta es solo una estructura temporal.
  console.log('Calculating age for:', birthDate);
  
  return {
    years: 0,
    months: 0,
    days: 0,
  };
};
